# Generated, never hand-edited — formatting it would produce a diff on every
# regeneration and hide the changes that matter.
packages/contracts/openapi.json
packages/dart/
apps/api/dist/
build/
.dart_tool/
pnpm-lock.yaml
node_modules/

# Prose. Markdown here is written to be read in a diff and in a terminal, and a
# formatter re-wrapping paragraphs turns a one-sentence edit into a fifty-line
# change nobody can review.
**/*.md

# Platform scaffolding generated by the Flutter and Xcode toolchains. Ours to
# keep working, not ours to restyle.
android/
ios/
linux/
macos/
windows/
web/
public/
firebase.json
.firebase/

# Dart has its own formatter and its own analyser config. `melos run format`
# owns this side of the workspace; prettier reaching into it would mean two
# tools with opinions about the same files.
lib/
test/fixtures/
analysis_options.yaml
pubspec.yaml
devtools_options.yaml
