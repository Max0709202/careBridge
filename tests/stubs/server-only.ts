/**
 * Test stub for the `server-only` package.
 *
 * In a real build, importing `server-only` from a Client Component is a build
 * error — that guard stays exactly as it is. Under Vitest there is no client
 * graph, so this empty module stands in for it.
 */
export {};
